package com.example.sprinklr.marketplace.infrastructure.outbound.mcp.red;

/**
 * A dotted filter field path extracted from a RED sample response, with type and an example value.
 */
public record RedSampleFilterPath(String type, String example) {
}
